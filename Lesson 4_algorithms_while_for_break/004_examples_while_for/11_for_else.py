for i in range(3):  # цикл буде повторено три рази, якщо користувач не завершить його раніше
    response = input('Введіть stop, щоб зупинити цикл (інакше що завгодно): ')

    if response == 'stop':
        break
else:
    # ця гілка буде виконана тільки якщо цикл не було перервано
    print('Цикл завершився сам')

print('Кінець програми')
